{
  "name": "seeuaround-app",
  "private": true,
  "version": "0.1.0",
  "description": "See U Around — mobile app and API",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @seeuaround/api",
    "dev:mobile": "npm run start -w @seeuaround/mobile",
    "build:api": "npm run build -w @seeuaround/shared && npm run build -w @seeuaround/api",
    "migrate": "npm run migrate -w @seeuaround/api",
    "typecheck": "npm run typecheck -w @seeuaround/mobile && npm run typecheck -w @seeuaround/api"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.2",
    "react-native-screens": "~4.26.0",
    "react-native-reanimated": "4.5.1",
    "react-native-worklets": "0.10.1",
    "expo-constants": "~57.0.14"
  }
}
