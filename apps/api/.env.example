DATABASE_URL=postgresql://user:pass@localhost:5432/seeuaround
APP_URL=http://localhost:3001
WEB_URL=https://seeuaround.com
EXPO_ACCESS_TOKEN=
RUN_MIGRATIONS=true
NODE_ENV=development
PORT=3001
# USE_PRODUCTION_AUTH=true   # require Resend + real OTP in production
# RESEND_API_KEY=             # when set with USE_PRODUCTION_AUTH, sends real codes
# DEV_AUTH_CODE=123456        # fixed OTP in dev only (never in NODE_ENV=production)
